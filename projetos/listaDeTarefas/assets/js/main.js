document.addEventListener('submit', function(e)
{
    e.preventDefault();

    const recebeTitulo = document.querySelector('titulo');
    const recebeDescricao = document.querySelector('descricao');

    let titulo = recebeTitulo
})